# Bootstrap only — all platform configuration lives in Admin → Settings
# http://localhost:3002/settings
#
# After saving settings in Admin, restart dev servers so `.fosl-runtime.json` is loaded.
# Optional: keep DATABASE_URL here for first-time DB setup before Admin is configured.
# DATABASE_URL="mysql://fosl:password@localhost:3306/fosl_dev"
